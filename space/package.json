{
  "name": "space-sim",
  "version": "1.0.0",
  "private": true,
  "description": "Two space-sim builds (iii + claude) on one selectable page.",
  "scripts": {
    "dev": "vite . --open",
    "preview": "vite preview"
  },
  "devDependencies": {
    "vite": "^5.4.0"
  }
}
